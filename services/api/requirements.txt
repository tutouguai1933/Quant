fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.0,<3
httpx>=0.27,<1
python-multipart>=0.0.9
ccxt>=4.0,<5

# ML 模型依赖
# 注意：xgboost 单独安装（见 Dockerfile），用 --no-deps 避免拉 303MB 的
# nvidia-nccl-cu12（仅 CUDA 需要，服务器无 GPU）；numpy/scipy 由
# scikit-learn 传递提供。
lightgbm>=4.0.0
optuna>=3.4.0
joblib>=1.3.0
scikit-learn>=1.3.0

# qlib 存在性探测占位：自研训练管线不依赖官方 qlib（重依赖不适合 1.6G 服务器），
# 但 qlib_config 用 find_spec("qlib") 判断 backend 显示为 "qlib" 还是 "qlib-fallback"。
# 安装轻量同名包即可让 backend 正确显示。
qlib==0.0.2.dev20
