# 研究到执行一体化工作台实施计划

关联设计：

- [2026-04-06-research-to-execution-workbench-design.md](/home/djy/Quant/.worktrees/hardening-phase1/docs/superpowers/specs/2026-04-06-research-to-execution-workbench-design.md)

## Scope

- 显性化六个工作台：
  - 数据工作台
  - 特征工作台
  - 策略研究工作台
  - 回测工作台
  - 评估与实验中心
  - 执行与自动化工作台
- 复用现有后端主链，不重做执行器或研究器

## Out of Scope

- 多市场
- 高频
- 新研究框架
- 全自动无人值守实盘

## Step 1：数据工作台（已完成）

目标：

- 把数据来源、时间范围、样本数量、快照 ID、数据状态正式显性化

任务：

- 新增数据工作台 API 聚合入口
- 新增数据工作台页面
- 支持查看：
  - 数据来源
  - 币种
  - 周期
  - 时间范围
  - 样本数量
  - 快照 ID
  - `raw / cleaned / feature-ready`

验收：

- 页面能回答“这次研究到底用了什么数据”

## Step 2：特征工作台（已完成）

目标：

- 把因子层从“后端存在”变成“前端可见”

任务：

- 新增特征工作台 API 聚合入口
- 新增特征工作台页面
- 支持查看：
  - 因子组
  - 主判断因子
  - 辅助确认因子
  - 预处理规则
  - 参数映射
  - 特征版本

验收：

- 页面能回答“当前启用了哪些因子、为什么这么配”

## Step 3：策略研究工作台（已完成）

目标：

- 把训练、推理、标签和实验参数显性化

任务：

- 新增策略研究工作台页面
- 支持查看：
  - 策略模板
  - 标签方式
  - 持有周期
  - 训练/验证/测试窗口
  - 当前模型
  - 当前实验参数
- 第一版先支持最小手动选择：
  - 币种
  - 周期
  - 标签模板
  - 研究模板

验收：

- 页面能回答“这次实验到底在研究什么”

## Step 4：回测工作台（已完成）

目标：

- 把成本、动作段和结果曲线显性化

任务：

- 新增回测工作台 API 聚合入口
- 新增回测工作台页面
- 展示：
  - 收益
  - 净收益
  - 成本影响
  - 回撤
  - Sharpe
  - 动作段
  - 买卖切换次数

验收：

- 页面能回答“回测结果为什么看起来这样”

## Step 5：评估与实验中心（已完成）

目标：

- 把推荐理由和淘汰理由做成可比较中心

任务：

- 新增评估与实验中心页面
- 展示：
  - 实验排行榜
  - 候选对比
  - 推荐原因
  - 淘汰原因
  - 样本外稳定性
  - 进入 `dry-run` / `live` 的门槛

验收：

- 页面能回答“为什么推荐这个币，为什么淘汰那个币”

## Step 6：执行与自动化工作台收口（已完成）

目标：

- 继续承接研究结果，不让主路径断开

任务：

- 在执行页和自动化页补上来自研究层和评估层的入口
- 保持：
  - 推荐候选
  - 执行状态机
  - 健康摘要
  - 调度顺序
  - `dry-run only`
  - `Kill Switch`

验收：

- 用户能从研究工作台自然进入执行与自动化工作台

## 当前追加收口（已完成）

目标：

- 把前五个工作台从“能看结果”继续推进到“能配关键参数”

任务：

- 新增统一工作台配置中心 `GET /api/v1/workbench/config`、`POST /api/v1/workbench/config`
- 数据工作台支持配置：
  - 研究标的
  - 主标的
  - 研究周期
  - 样本长度
- 特征工作台支持配置：
  - 主判断因子
  - 辅助因子
- 策略研究工作台支持配置：
  - 研究模板
  - 模型
  - 标签阈值
  - 持有窗口
- 回测工作台支持配置：
  - 手续费
  - 滑点
- 评估与实验中心支持配置：
  - `dry-run` 门槛
  - `live` 门槛
- 研究链、回测链和自动小额 `live` 会真正消费这些配置，而不是只做前端展示
- fallback / unavailable 场景下禁用配置提交，避免默认兜底值覆盖真实配置
- 研究和评估页面显性展示“当前结果与配置是否对齐”
- 明确区分“未配置时沿用默认因子”和“用户主动清空因子”
- 让严格研究模板真正进入规则门和打分逻辑，而不是只显示在页面上

验收：

- 用户能在页面上改配置
- 配置保存后，后端配置和页面默认值一起变化
- 下一轮训练、推理、回测和自动 live 门会按新配置生效
- 工作台不可用时不会再把 fallback 默认值提交回后端
- 研究页和评估页会明确提示当前结果是否已过期

## Phase 2 Todo（持续更新）

- [x] 数据工作台支持滚动窗口和固定日期范围
- [x] 特征工作台支持主判断因子、辅助因子和预处理规则配置
- [x] 特征工作台支持 1h / 4h 周期参数显性化
- [x] 策略研究工作台支持研究模板、模型、标签方式和持有窗口配置
- [x] 策略研究工作台支持研究分数权重和最低置信度配置
- [x] 回测工作台支持手续费、滑点和成本模型配置
- [x] 回测工作台支持 dry-run / live 门槛和规则门配置
- [x] 评估中心支持最近训练、最近推理和最近两轮实验对比
- [x] 评估中心支持推荐原因、淘汰原因和配置差异拆解
- [x] 评估中心支持规则门、验证门、回测门、一致性门、live 门开关
- [x] 任务页支持长期运行参数、告警窗口和人工接管时间线
- [x] 任务页支持执行安全门：白名单、单笔金额、最大持仓数
- [x] 研究动作支持后台运行状态、阶段、进度、预计时长和结果去向
- [x] 浏览器测试恢复到真实登录、真实提交、真实样式资源验证
- [x] 数据工作台补更多数据质量细项：坏行、缺失行、快照来源解释
- [x] 特征工作台补更多细粒度因子控制：按因子单项说明和类别权重解释
- [x] 研究工作台补更完整模型说明：当前模型适用场景和标签影响说明
- [x] 回测工作台补更完整结果拆解：动作段明细、成本来源和过滤影响
- [x] 评估中心补更清楚的研究/执行差异条目与修复动作
- [x] 任务页补更直白的告警摘要和人工接管快捷动作
- [x] 长期运行入口补更稳定的调度状态与恢复确认
- [x] 研究动作和工作台保存统一改成服务端直连控制面 API，避免前端代理链路导致“客户端代理暂时不可用”
- [x] 任务页新增“最近工作流来源”，手动信号流水线会写入 AutomationService 并对 `/tasks` 做反馈，AutomationService 支持按告警 id 确认与按级别清理
- [x] 任务页“本轮自动化判断”改成实时卡片，手动信号流水线无需整页刷新也能同步到任务页
- [x] 前端验证改成串行：先停 dev 再 build，再启动最新前端，避免 `.next` 缓存互相破坏
- [x] 真实点击“运行 Qlib 信号流水线”后，研究运行状态、任务页来源和结果去向都重新验通
- [x] 浏览器全量测试切到“构建完成后再验证”，避免 build 和页面验证并发打坏样式与资源

## Phase 2 Todo（第二段）

- [x] 评估中心补上“准入预设”，让 dry-run / live 门槛能按整套口径快速切换
- [x] 评估中心补上“自选实验对比”，让用户能手动挑两轮实验直接比较
- [x] 任务页补上“告警快捷处理”，支持确认头号告警和清理非错误告警
- [x] 告警处理链改成不再追加新的 info 告警，避免用户清理后列表里立刻又出现一条新提示

## Phase 2 Todo（第三段）

- [x] 特征工作台补上类别权重配置，并把趋势/动量/量能/震荡/波动权重显性化
- [x] 评估中心补上“分阶段最佳候选”，区分更适合进入 dry-run 和 live 的候选
- [x] 评估中心补上“研究 / 回测 / 执行对照”，直接说明三层结果差在哪
- [x] 任务页补上“调度顺序矩阵”和“失败规则矩阵”，把长期运行顺序和失败处理讲清楚
- [x] 浏览器全量验证重新跑通，并确认最新构建下 `/features`、`/evaluation`、`/tasks` 样式和交互正常

- [x] 研究工作台补上模型目录、标签方式目录、触发基础目录和持有窗口目录，并标出当前选中项
- [x] 研究工作台补上训练/验证/测试切分预览，让当前样本和切分比例能一起看
- [x] 任务页补上“长期运行与人工接管策略”矩阵，直接说明什么时候暂停、什么时候接管、什么时候恢复

## Validation Commands

```bash
source /home/djy/miniforge3/etc/profile.d/conda.sh
conda activate quant
cd /home/djy/Quant
python -m unittest discover -s services/api/tests -v
python -m unittest discover -s services/worker/tests -v
python -m unittest discover -s tests -v
```

```bash
cd /home/djy/Quant/apps/web
pnpm build
```

## Expected Results

- 后端测试继续通过
- 前端测试继续通过
- 页面新增后仍然保持主链可走通
- 用户能明确看见：
  - 数据
  - 特征
  - 策略研究
  - 回测
  - 评估
  - 执行与自动化
