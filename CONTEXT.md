# 当前进度

- 当前正在做：继续把自动化工作流的结果讲清楚，这轮已经在任务页新增“本轮自动化判断”，会直接显示推荐标的、推荐策略实例、派发结果、失败原因、本轮说明和下一步，不用再手工翻任务列表拼信息。
- 当前正在做：把“取长补短”的系统补强路线正式落进计划文档，这轮已新增 `docs/superpowers/plans/2026-04-06-quant-system-hardening-implementation.md`，明确后续按数据层、因子层、回测层、评估与复盘层、研究层、执行层、自动化层的顺序推进。
- 当前正在做：补系统导览文档，这轮已把研究、筛选、dry-run、live、自动化、复盘的整条流程写成 `docs/system-flow-guide.md`，方便后续快速熟悉系统。
- 当前正在做：收口前端本地地址口径，这轮已把登录、动作回跳、退出登录统一改成跟随当前请求主机名生成绝对地址，不再在前端运行时混用 `localhost` 和 `127.0.0.1`。
- 当前正在做：登录页已去掉明文密码占位，登录按钮不会再因为前端提前禁用而卡在“登录中…”，真实回跳现在会稳定落到 `http://127.0.0.1:9012/...`。
- 当前正在做：自动化工作流返回现在会显式带出 `message / failure_reason`，手动模式、筛选被拦、等待 dry-run、live 完成、训练或推理失败都会给出统一说明。
- 当前正在做：本地 `9011 / 9012` 已按最新代码重启；这轮已做真实任务页验证，生产构建通过，登录后任务页已确认能看到“本轮自动化判断 / 推荐策略实例 / 派发结果 / 失败原因”。
- 当前正在做：把自动化入口继续前移到主页面，这轮已在信号页和策略页补上“当前模式 / 最近一轮 / 自动化推荐 / 下一步动作”，不需要只盯任务页才能看懂自动化状态。
- 当前正在做：本地已按生产构建重新启动 `9011 / 9012`；真实页面验证已确认信号页会显示“自动化入口”，策略页会显示“自动化判断 / 自动化推荐 / 下一步动作”。
- 当前正在做：收口 Qlib 研究动作的真实可用性，这轮已经把研究运行目录改成自动创建，首次点击“运行 Qlib 信号流水线 / 研究训练 / 研究推理”时不再因为 `/tmp/quant-qlib-runtime` 不存在而直接失败。
- 当前正在做：统一补上前端表单动作的“运行中”反馈，首页、信号页、策略页、任务页、登录页和退出登录现在都会在提交瞬间显示进行中提示，不再像没反应。
- 当前正在做：本地 `9011 / 9012` 已按最新代码重启；真实验证已确认信号页点击“运行 Qlib 信号流水线”时会先显示“运行中”，Playwright 按钮交互检查已通过。
- 当前正在做：收口自动化主链路，这轮已经补上自动化状态持久化、本地研究训练入口统一、以及“研究模板 -> 策略实例”映射，自动 dry-run 和自动小额 live 不再一律塞回 `strategy_id=1`。
- 当前正在做：继续把自动化结果解释收口到任务页，这轮已经补上“本轮自动化判断”卡片，会直接显示推荐标的、推荐策略实例、派发结果、最近订单、失败原因、本轮说明和下一步动作。
- 当前正在做：本地 `9012` 这轮已改成先停掉旧 `pnpm dev`，再做干净 `pnpm build`，最后用 `pnpm start` 重新启动；这样任务页的真实验证不再被旧 `.next` 产物干扰。
- 当前正在做：任务页真实页面验证已补到 Playwright，当前会直接通过真实登录接口拿会话 cookie，再打开 `/tasks` 检查“本轮自动化判断 / 推荐策略实例 / 派发结果 / 失败原因”。
- 当前正在做：本地 `9011 / 9012` 已按最新代码重启，真实页面已确认任务页会显示“自动化控制台 / 统一调度入口 / 健康摘要”，暂停和恢复动作都会返回正确反馈。
- 当前正在做：自动化跑一轮现在仍会真实触发训练和推理，所以响应时间会比普通按钮长；当前快速验证已确认任务页 HTML、登录态、暂停/恢复反馈和自动化状态接口都正常。
- 当前正在做：收口前端导航和终端页面的稳定性，这轮已经把站内整页跳转改成客户端导航、补上路由切换骨架、修掉 `Badge` 组件导致的 hydration 结构错误，并在研究候选板加入“流程开关 / 门控视图”前端筛选条。
- 当前正在做：本地 `9012` 前端已经重新启动并恢复正常；真实浏览器验证已确认侧边栏在 `signals -> market -> balances -> signals` 间切换可完成，不再出现整页 500 和控制台报错。
- 当前正在做：临时放开研究筛选门里的“强制验证首选候选”，先把本地 `Qlib -> 筛选 -> dry-run -> 小额 live -> 复盘` 真正跑通；这轮已经确认通用 `Qlib` 信号会继承策略页当前执行器的策略上下文，不再被风控误判成“strategy not found”。
- 当前正在做：本地真实链路已重新验证成功，这次新的真实单是 `ETH/USDT`，`Freqtrade live` 已记录到一笔新的打开仓位，Binance 订单号是 `45491387738`。
- 当前正在做：为避免继续自动动作，本地 `Freqtrade` 已执行 `stopbuy` 暂停新入场；当前本地账户侧能看到 `ETH` 持仓、`USDT` 余额和历史 `DOGE` 零头。
- 当前正在做：把 `Qlib` 研究层从“最小闭环”继续收紧成“可复盘、可对比、可回看”的稳定流水线，这一轮已经补上数据快照、实验账本、带成本的最小回测、统一复盘报告和执行健康摘要。
- 当前正在做：为了先把本地真实链路跑通，临时开启“强制验证首选候选”模式；当前已在本地真实完成一次 `Qlib -> 筛选 -> live -> 复盘`，并暂停了执行器避免继续自动进场。
- 当前正在做：按 `shadcn/ui v4` 拆解报告继续收前端，已经切到“专业交易终端外观 + 决策优先”的双栏终端布局，并补上真实页面审计链。
- 当前正在做：本地重新验证 `Qlib -> 筛选 -> dry-run -> 小额 live -> 复盘`，这次已经确认研究训练、研究推理、统一研究报告、复盘接口都能跑通；当候选全部被研究门拦住时，流程会正确停在“继续研究”，不再误派发旧 mock 信号。
- 当前正在做：按实现计划推进 `Qlib` 单币择时研究工厂，已完成研究数据集切分、最小择时因子/标签、规则层门控、最小回测、候选排行与 dry-run 准入门，并已把研究工厂结果接入 API、信号主链路、页面展示和统一研究报告出口。
- 当前正在做：继续把 `Qlib` 研究筛选、实验对比和执行推荐收紧，当前已经把训练验证门真正接进候选排行和执行推荐主链。
- 当前正在做：把“自动化运维 + 自动买卖”收成正式 spec 和实现计划，作为下一阶段主线。
- 上次停留位置：阿里云上的 `API / WebUI / Freqtrade / Mihomo` 四个容器已经稳定运行，真实余额、真实订单和真实持仓都能从服务器读到。
- 近期关键决定：
  - 前端导航这轮固定为三条规则：
    - 站内链接优先用 `next/link`，不再用原生 `<a>` 触发整页重载
    - 路由切换统一显示 `app/loading.tsx` 骨架，避免切换时看起来“卡死”
    - 研究门、回测门、验证门和强制验证入口先做成前端筛选开关，用来快速看候选，不直接改后端规则
  - 自动化这轮新确认：
    - 自动化状态现在会落到本地状态文件，重启后不会丢掉当前模式、暂停状态和最近 armed 候选
    - 自动化推荐候选会按 `strategy_template` 映射到真实策略实例编号，不再一律回退到 `strategy_id=1`
    - `/api/v1/tasks/train` 现在走统一研究训练，不再调用旧的 mock 信号流水线
    - 自动化最近一轮的关键判断现在会直接收口到任务页右栏，不再需要用户手动翻任务列表和复盘结果拼信息
    - 本地生产构建前如果已有 `pnpm dev` 正在占用 `.next`，需要先停旧进程并重建产物，否则会出现构建阶段找不到旧 chunk 的假故障
  - 为了先验证完整主链，临时加入 `QUANT_QLIB_FORCE_TOP_CANDIDATE=true`：当研究候选全部被筛选门拦下时，只允许分数最高的 1 个候选进入验证链，其余候选仍保持原筛选结果不变。
  - 通用 `Qlib` 信号在从策略页派发时，会继承当前执行器策略 `1` 作为风控和执行上下文；这样研究信号不会再因为缺少 `strategy_id` 被风控层直接拦下。
  - `WSL` 继续负责开发和测试，阿里云服务器负责真实 `dry-run / live` 验证和最终部署。
  - 为了先验证本地真实链路，研究筛选门当前支持“只临时放行分数最高候选进入验证链”，但 `live` 硬安全门仍然保留。
  - 下一阶段路线已经固定：
    - 先自动化训练、推理、同步、复盘
    - 再自动化 `dry-run`
    - 最后在硬风控下自动化小额 `live`
  - 对应文档已经新增：
    - `docs/superpowers/specs/2026-04-04-automation-ops-and-auto-trading-design.md`
    - `docs/superpowers/plans/2026-04-04-automation-ops-and-auto-trading-implementation.md`
  - 服务器端口继续按 `/home/djy/.port-registry.yaml` 统一管理，`Quant` 主范围固定，临时联调用 `Quant-Debug-N`。
  - Binance 在服务器上通过固定代理节点访问；当前固定节点仍要用它的真实出口 IP 做白名单。
  - 本地真实验证已经确认：策略页派发 `Qlib` 通用信号时，必须显式继承执行器策略上下文，否则风控会把它误判成“strategy not found”。
  - `Freqtrade` 当前已经确认：
    - 真实 `Spot + dry-run + REST` 已跑通
    - 首笔真实 `DOGE/USDT` 买单已经成交
    - `live` 同步会直接用 Binance 账户结果确认刚派发的订单
    - `flat` 只会收敛到当前币种或当前交易，不会全平
  - 这次新确认：
    - 本地 `Qlib -> 训练 -> 推理 -> 统一研究报告 -> live -> 复盘` 已真实跑通一轮
    - 这轮临时放行的最优候选是 `ETHUSDT`
    - 本地新的真实买单已经成交，Binance 订单号是 `45491387738`
    - 当前本地余额和持仓已经更新：
      - `ETH` 持仓约 `0.00289710`
      - `USDT` 可用余额约 `14.04719936`
    - 本地复盘接口已返回：
      - `training=completed`
      - `inference=completed`
      - `screening=succeeded`
      - `live=succeeded`
    - 当前本地 `Freqtrade` 已切回暂停态，避免继续自动开仓
    - 阿里云服务器已经切到 `live` 并验证过真实余额同步
    - 今天又成功发起了一笔新的真实 `DOGE/USDT` 买单，订单号是 `14140438880`
    - 这笔新单已经被 `Freqtrade live` 正确记录，没有和昨天 `2026-04-02 16:20:58` 的旧单混在一起
    - 真实卖出失败的根因已经确认：`DOGEUSDT` 的 `LOT_SIZE` 要求整数数量，`1 USDT` 的小仓位在手续费和最小成交额一起作用下会出现“买得进、卖不干净”
    - 现在 live 安全门已经补上“最小可卖出金额”检查，后续不会再允许这种小仓位进入 live
    - 服务器上的 live 单笔上限和执行器默认 stake 现在都已经切到 `6 USDT`
    - 这次已经按用户确认做了真实卖出：
      - Binance 上成功卖出 `23 DOGE`
      - 新卖单订单号是 `14140484509`
      - 卖出后 `USDT` 可用余额回到 `20.00771436`
      - `DOGE` 还剩 `0.97600000`
    - 这 `0.976 DOGE` 不是系统再次卡单，而是 Binance `DOGEUSDT` 的步长规则留下的零头
    - `Freqtrade live` 里那笔不该继续算“打开中”的记录已经删除，`/api/v1/status` 和 `/api/v1/trades` 现在都为空
    - 当前阿里云上的 live 执行器仍然保持 `stopped`，避免继续产生新的真实动作
    - 余额接口和余额页现在已经能直接标出“可交易资产 / 零头资产”
    - 像 `0.976 DOGE` 这类余额会被明确显示为“交易所零头”，不会再和打开中的仓位混在一起理解
    - 图表页现在会直接展示 `EMA20 / EMA55` 和“最近图表点”
    - 市场页现在除了进入单币图表页，还会直接给出“进入策略中心”的下一步入口
    - 单币图表页现在会把“最近图表点”和“进入策略中心”放到主图下方，形成更清晰的下一步引导
    - 订单页和持仓页现在会明确提示：如果列表已空但余额页还有币，优先按“交易所零头”理解，不再误判成系统卡仓
    - 本地运行现在也统一遵循 `/home/djy/.port-registry.yaml`
    - 本地默认端口已经切到：`9011` API、`9012` WebUI、`9013` Freqtrade REST
    - 信号页和驾驶舱里的主按钮现在会直接运行 `Qlib` 信号流水线，不再默认走 `mock`
    - 同时保留“演示信号流水线”，专门给重复验证 `dry-run` 成功链路使用
    - 策略页的每张策略卡片现在会显示研究分数、研究解释和模型版本
    - 下一阶段已经确定：先做 `BTC / ETH / SOL / DOGE` 的单币择时，不做币种轮动
    - 研究层将采用“规则层 + ML 层双层门控”，持有周期聚焦 `1-3 天`
- 当前阻塞：
  - 真实买入和真实卖出可卖部分已经走通
  - `0.976 DOGE` 已经按交易所零头路径收口，不再作为打开仓位理解
  - 后续如果要继续真实闭环，应基于新的 `6 USDT` 配置重新开一笔干净仓位，再验证完整买卖
  - `Qlib` 单币择时设计 spec 已确认
  - `Qlib` 单币择时实现计划已落到 `docs/superpowers/plans/2026-04-03-qlib-single-asset-timing-factory-implementation.md`
  - `Task 1` 已完成：研究数据集会优先使用 `4h`，必要时回退 `1h`，并稳定切成训练/验证/测试三段
  - `Task 2` 已完成：最小择时因子已补齐，标签已改成真正的 `1-3d` 窗口语义，不再只是固定第 3 天终点
  - `Task 3` 已完成：规则层已经能按趋势、波动和量能做最小门控
  - `Task 4` 已完成：训练结果里已经带有 `validation` 和 `backtest`，最小回测会输出收益、回撤、Sharpe、胜率和换手
  - `Task 5` 已完成：候选排行会按分数输出，并为每个候选独立计算回测与 dry-run 准入，不再共用训练级结果
  - 这次同时把最小回测里的换手定义收紧为“动作段数量”，避免单币择时在稳定趋势里被误判成高换手
  - `Task 6` 已完成：研究工厂候选结果已经接入 API，支持读取候选总览和单币候选摘要
  - 这次同时修正了 API 训练入口的样本长度，研究层不再只拿 24 根 `1h` K 线，避免 `1-3d` 标签在真实接口里跑不起来
  - `Task 7` 已完成：`Qlib` 研究信号现在只有通过研究候选 `dry-run` 门后，才允许进入可派发队列
  - 这次没有改信号契约本身，而是在信号服务内部把研究候选门槛跟着信号一起保存，避免后续研究结果变化时把旧信号资格改写掉
  - `Task 8` 已完成：信号页、单币页、策略页现在都会展示研究候选、`dry-run` 准入状态和下一步动作；单币页同时修正了候选板重复渲染和空态文案缺失
  - 本地按端口注册表重新验证了 `9011 / 9012`，研究候选接口、信号页、单币页、策略页都已返回最新内容
  - `Task 9` 已完成：研究层现在新增统一研究报告出口 `GET /api/v1/signals/research/report`，会把训练、推理、候选和实验摘要一次收齐
  - 本地已重新启动 `9011` 验证这个新接口，返回正常
- 这次继续把 `Task 1` 和 `Task 3` 真正接进研究主链路：
    - 研究训练和推理现在都会同时准备 `1h / 4h` 两套 K 线
    - worker 主链路现在统一通过 `qlib_dataset.build_dataset_bundle` 做时间切分，不再在 runner 里手写切分逻辑
    - 推理候选现在会真实消费 `qlib_rule_gate.evaluate_rule_gate`，规则门会和回测门一起决定是否允许进入 `dry-run`
    - 为兼容旧测试和旧调用，runner 仍接受单周期旧输入，但内部会先推断周期再转成统一数据集包
  - 研究筛选与验证新计划前四步已推进完成：
    - `Task 1`：研究筛选门已收紧，新增样本量和连续亏损段约束
    - `Task 2`：worker 侧已有统一实验对比摘要，训练、推理、候选和实验摘要走同一出口
    - `Task 3`：API 研究报告已复用 worker 统一实验摘要，不再各处手工拼装
    - `Task 4`：策略工作台已给出当前研究推荐候选，执行链会优先认领当前推荐且允许进入 `dry-run` 的研究信号
    - `Task 5`：`dry-run -> 小额 live -> 复盘` 验证工作流已固定到 README、计划和研究运维文档
    - `Task 6`：信号页、单币页、策略页现在都会显示统一研究报告、研究候选和下一步动作
    - 这次已按 `9011 / 9012` 做真实页面验证：
      - 信号页已显示“统一研究报告 / 最近实验摘要”
      - 单币页已显示“返回信号页继续研究”
      - 策略页在带会话令牌请求时，已能进入“已解锁”状态
  - 这次继续把研究筛选和回测结果讲清楚：
      - 统一研究报告现在会显示 `blocked_count / pass_rate_pct / top_candidate_symbol / top_candidate_score`
      - 训练实验摘要现在会带出统一回测摘要，包含收益、回撤、Sharpe、胜率、换手和连续亏损段
      - 候选板现在会直接显示“筛选通过率 / 当前最佳候选 / 回测收益 / 最大回撤 / Sharpe / 失败原因”
      - 本地已重新跑通一轮 `研究训练 -> 研究推理`，报告里当前最佳候选为 `ETHUSDT`，筛选通过率为 `25.00%`
  - 这次本地完整验证又确认了两个真实问题并已修掉：
    - `/api/v1/tasks/validation-review` 不再被 `/{task_id}` 路由吞掉，HTTP 现在会正常返回复盘报告
    - 当 `Qlib` 候选已经进入待处理状态但全部未通过研究门时，派发链不再回落到旧 mock 信号
  - 当前本地这轮真实验证结果：
    - `Qlib` 训练和推理都能完成
    - 统一研究报告会给出 `recommended_action=continue_research`
    - 派发会返回 `signal_not_ready`
    - 复盘步骤会明确停在 `screening failed / dry_run waiting / live waiting`
  - 这次没有放宽执行安全边界：
    - 研究推荐只影响“优先顺序”和页面下一步动作
    - 仍然不能绕过现有 `dry-run` 准入门和派发安全门
  - 下一阶段开发顺序已明确并落进新计划：
    - 先继续把研究筛选和验证结果收成更稳定的页面入口
    - 再继续补更细的回测对比和研究复盘视图
  - 对应计划文件已新增：`docs/superpowers/plans/2026-04-03-qlib-screening-and-validation-implementation.md`
  - 针对整轮 `Task 1-9` 的整体 review，已修正两处关键问题：
    - 研究运行目录为空时，不再误报 `ready`
    - 统一研究报告里的 `signal_count` 现在会对 `signals` 列表做回退，不再和推理结果自相矛盾
  - 同一轮 review 里还额外收紧了三处行为：
    - 顶层状态不是 `ready` 时，候选快照和研究报告都不再透出旧候选
    - 候选 `candidate_count / ready_count` 会和实际候选列表自动对齐
    - 前端研究候选 fallback 不再伪造“有 1 个 ready 候选”
  - 训练样本不足以形成验证和回测时，现在会直接报错，不再落出半完成的 `completed`
  - 下一步可以继续深化研究报告在页面中的使用，或把统一研究流程补到页面动作里
  - 这次前端已正式切到新的设计方向：
    - 采用“专业交易终端外观 + 决策优先动线”
    - 设计规则文档已写入 `docs/superpowers/specs/2026-04-04-decision-first-terminal-ui-design.md`
    - 实现计划已写入 `docs/superpowers/plans/2026-04-04-decision-first-terminal-ui-implementation.md`
  - 这次已完成第一批终端化重构：
    - `AppShell` 已切到深色终端风格和“当前工作区”摘要条
    - 首页已改成“左侧推荐动作 / 右侧当前决策板”
    - 信号页已改成“左候选 / 右统一研究报告”
    - 策略页已改成“左判断 / 右执行”的双栏布局
  - 这次已完成真实页面验证：
    - `9012 /` 已显示新的终端化首页结构
    - `9012 /signals` 已显示“研究终端 / 筛选通过率 / 当前最佳候选”
    - `9012 /market/BTCUSDT` 已换到新的终端壳层
    - `9012 /strategies` 未登录态已显示新的终端化标题和布局说明
  - 这次还确认了一个本地运行现象：
    - 当前机器上 `pnpm dev` 可正常做真实页面验证
    - `pnpm start` 偶发读取到坏掉的旧 `.next` 产物，这不是本轮页面代码本身的问题，后续要单独收口本地前端运行环境
  - 这次新增了一份 `shadcn/ui v4` 拆解报告：
    - 路径：`docs/research/2026-04-04-shadcn-ui-v4-decomposition.md`
    - 核心结论：`shadcn/ui` 的价值不在“单个组件”，而在“主题变量 + 页面壳层 + 功能块 + 示例组合”
    - 后续如果继续收前端，不应只抄基础组件，而应优先参考官网完整页面、dashboard block、data table 和 chart 示例
  - 这次前端重构还新增了统一 UI 基础层：
    - `apps/web/components/ui/` 已加入按钮、卡片、标签、分隔线、输入框和选项卡
    - 首页、信号页、策略页、余额页、订单页、持仓页、登录页已全部切到新的终端化布局
    - 根级 `404` 页面已补齐，生产构建不再因为 `/_not-found` 缺失而失败
  - 这次前端质量验证已经固定为：
    - 仓库级测试
    - `pnpm build`
    - Playwright 的布局、控制台、网络和 axe 审计
    - Lighthouse 的 `accessibility / best-practices`
  - 这次本地已重新验证：
    - 生产构建成功
    - Playwright 前端审计 `20` 通过、`0` 失败
    - Lighthouse 在 `/signals` 和 `/market/BTCUSDT` 上都达到 `accessibility=1`、`bestPractices=1`
    - 登录后的 `/strategies` 页面已确认显示“已解锁 / 策略中心 / 当前推荐执行候选”
  - 这次单独修了 `ETHUSDT` 单币页看不到 K 线的问题：
    - 根因不是接口没数据，而是主图区过度依赖外部交互图表脚本
    - 现在主图区已经改成“本地主图优先、交互增强次之”
    - 即使交互增强脚本暂时没连上，页面也会先显示稳定可见的本地 K 线、入场位和止损位
    - 本地已重新验证：
      - `tests.test_market_workspace`：`13` 通过、`0` 失败
      - `pnpm build`：通过
      - `ETHUSDT` 页面网络与控制台检查：`2` 通过、`0` 失败
      - `ETHUSDT` 页面布局与 axe 检查：`2` 通过、`0` 失败
  - 这次单独修了 `ETHUSDT` 单币页的图表主区：
    - 根因不是接口没数据，而是主图区把外部交互图当成唯一主角，脚本一旦不稳就像“没图”
    - 现在主图区已经改成“本地 K 线主图优先、交互增强次之”
    - 页面会先稳定显示本地主图、入场位、止损位和最近图表点
    - 交互增强图现在只作为第二层，不再决定这页是否可用
    - 本地已按 `9012/market/ETHUSDT` 做真实页面验证：
      - HTML 已出现“本地主图 / 交互增强 / 进入策略中心”
      - Playwright 网络与控制台检查 `2` 通过、`0` 失败
      - Playwright 布局与 axe 检查 `2` 通过、`0` 失败
  - 这次继续把单币页收成“上图下信息”：
    - 图表主区不再被右侧候选和研究卡继续压缩
    - 周期切换改到图表上方横向排布，不再左右各占一列
    - 单币页下半区再承接多周期摘要、研究侧卡、候选板和下一步动作
    - 同时把过长的研究门控状态标签改成短文案，解决了 `ETHUSDT` 页面横向溢出
    - 本地已重新验证：
      - 页面源码检查通过
      - `tests.test_market_workspace + tests.test_frontend_refactor`：`30` 通过、`0` 失败
      - `pnpm build`：通过
      - `ETHUSDT` 页面网络与控制台检查：`2` 通过、`0` 失败
      - `ETHUSDT` 页面布局与 axe 检查：`2` 通过、`0` 失败
  - 这次继续把 `ETHUSDT` 单币页压缩成更高效的一屏结构：
    - 页面正文不再单独再挂一整块研究候选板和下一步动作卡
    - 候选、研究分数、回测结果、研究门和执行动作已收进右侧紧凑“执行摘要”
    - 现在页面主路径是“先看本地主图，再看右侧执行摘要”，滚动明显减少
    - 本地已重新验证：
      - `tests.test_market_workspace + tests.test_frontend_refactor`：`30` 通过、`0` 失败
      - `pnpm build`：通过
      - `ETHUSDT` 页面网络与控制台检查：`2` 通过、`0` 失败
    - `ETHUSDT` 页面布局与 axe 检查：`2` 通过、`0` 失败
      - HTML 已出现“本地主图 / 执行摘要 / 研究分数 / 进入策略中心”
  - 这次继续把研究主链再收紧了一层：
    - 候选排行现在不只看规则门和回测门，还会看训练阶段的验证摘要
    - 验证样本过少、正收益占比太弱、验证期平均未来收益过差时，候选会直接停在“继续研究”
    - 统一研究报告现在会同时输出：
      - 推荐标的
      - 推荐下一步动作
      - 排行榜
      - 筛选失败原因汇总
    - `get_research_recommendation()` 现在即使没有可进入 `dry-run` 的候选，也会明确返回“继续研究”的推荐，而不是直接空掉
    - `Qlib` 信号写入主链时，已经会带上：
      - `review_status`
      - `next_action`
      - `execution_priority`
    - 这样执行链不会误把“还该继续研究”的候选当成可以直接派发的标的
  - 这轮本地回归已经完成：
    - Worker 测试：`35` 通过、`0` 失败
    - API 测试：`54` 通过、`0` 失败
    - 语法检查：通过
  - 这次继续把研究与验证主链再往前收紧了一层：
    - 研究运行目录现在会新增 `dataset/latest_dataset_snapshot.json` 和 `runs/experiment_index.json`
    - 训练和推理结果现在都会带出 `dataset_snapshot / dataset_snapshot_path`
    - 统一研究报告现在会同时带出最近实验账本 `recent_runs`
    - 最小回测现在已经会扣最基础的手续费和滑点，统一输出 `gross / net / cost_impact`
    - 候选筛选门现在优先看净收益，并增加了“验证和净回测漂移过大”的阻断理由
    - 统一研究报告里的筛选失败原因现在会按 `rule / validation / backtest / consistency` 四类分组
    - 控制平面新增了统一复盘出口：`GET /api/v1/tasks/validation-review`
    - 控制平面也新增了主动生成复盘任务：`POST /api/v1/tasks/review`
    - 任务调度器现在会记录最近成功/失败的 `train / sync / reconcile / review` 状态
    - 执行链现在会输出 `execution_health`，明确最近同步成功时间、最近失败同步和当前是否陈旧
  - 这轮本地回归已经完成：
    - Worker 测试：`44` 通过、`0` 失败
    - API 测试：`47` 通过、`0` 失败
    - 语法检查：通过
