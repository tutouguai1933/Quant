# 当前进度

- 当前正在做：phase2 继续收口；这轮把“运行 Qlib 信号流水线”的真实点击链从登录、提交、研究运行状态到任务页来源都重新验了一遍，同时把前端验证彻底改成“先构建、再重启、再跑浏览器全量”的串行流程。
- 上次停留位置：6 个工作台已经显性化并支持第一批配置项；评估页能看最近两轮对比，任务页能看长期运行参数、告警和人工接管信息。
- 近期关键决定和原因：
  - `/actions` 和工作台配置保存现在统一改成服务端直连控制面 API，不再绕前端 `/api/control` 代理；这样按钮提交和配置保存不会再因为 Next 代理链路抖动直接报“客户端代理暂时不可用”。
  - 这轮前端真实验证除了构建和页面 HTML，还补了真实点击“运行 Qlib 信号流水线”的交互路径，确保会出现成功反馈，并且研究运行状态区能同步进入后台阶段。
  - 这轮完整验证结果：后端定向测试 `35` 通过、`0` 失败；Playwright 浏览器测试 `4` 通过、`0` 失败；前端生产构建通过；真实点击 `/signals -> 运行 Qlib 信号流水线` 已确认成功反馈和运行状态同时可见。
  - 研究页现在显式开放持有窗口选择；回测页显式开放成本模型选择；评估页显式开放规则门、验证门、回测门、一致性门和 live 门开关，避免后端已经支持、前端仍然看不见。
  - 前端 fallback 数据必须跟真实类型同步补齐；否则生产构建会在类型检查阶段失败，看起来像页面问题，实际是兜底模型缺字段。
  - 浏览器测试默认端口这轮统一切到 `9021/9022`，这样本地调试和真实页面验证不再每次都手动带环境变量。
  - 工作台配置继续只走统一配置中心，不再给数据、特征、研究、回测、自动化各开一套保存逻辑，避免配置漂移。
  - 研究结果和当前配置是否一致，统一通过 `config_alignment` 和“最近两轮对比”显性化，避免只看到结论、看不到上下文。
  - 研究训练、推理、流水线统一改成后台运行，并显式给出阶段、进度、预计时长和结果去向，避免“点了按钮不知道系统在干什么”。
  - 如果实验账本缺少配置快照，页面必须明确显示“暂时无法比较”，不能再误写成“没有变化”。
  - 自动化健康摘要现在必须把同步失败和人工接管分开讲清楚，所以后端统一补了 `last_failure_at / last_sync_failure_at / paused_since / takeover_since / sync_failure_count`，前端直接按这套字段渲染。
  - 长期运行能力这轮继续保持“先把真实状态讲清楚，再放更多自动化动作”，所以任务页优先展示同步失败细节、人工接管时间线、长期运行参数和执行安全门，而不是继续堆按钮。
  - Playwright 浏览器测试现在优先使用 Playwright 自带浏览器，并通过 `QUANT_WEB_BASE_URL / QUANT_API_BASE_URL` 适配调试端口；前端关键交互验证恢复可用。
  - 单币页图表数据如果基础 K 线为空，现在直接按空态返回，不再继续串行拉多周期摘要；这样 `/market/[symbol]` 不会因为上游无数据而整页卡住。
  - 信号页和登录页的浏览器测试现在拆成“两层”：一层只测运行中反馈，直接阻止表单离开当前页；另一层再测最终跳转结果，避免导航时序把真实交互误报成失败。
  - 表单按钮的运行中反馈现在同时覆盖点击提交和 Enter 提交；表单开始提交时就会进入运行中，不再只靠按钮点击分支。
  - 表单按钮的提交改成先同步渲染运行中，再延迟触发表单提交，避免按钮节点切换时触发浏览器异常。
  - 前端重新构建后必须重启 `next start`，否则旧进程会继续引用失效 chunk，页面会退化成纯文字；这轮已经按这个规则重启了 `9022`。
  - 本地 `9021/9022` 这轮已用最新构建重启，并补做了浏览器级验证；当前 `/signals`、`/market/BTCUSDT`、`/evaluation`、`/tasks` 都能正常打开。
  - 这轮完整验证结果：前端相关 Python 测试 `45` 通过、`0` 失败；Playwright 浏览器测试 `36` 通过、`0` 失败；前端生产构建通过。
  - 任务页已补上长期运行参数、告警窗口和人工接管相关信息；策略页已补上当前配置摘要；评估页已把配置变化字段统一成人能看懂的中文标签。
  - phase1 的验收可以视为收口完成；当前正式进入 phase2，中点目标是把工作台继续升级成“更完整可配置、实验更容易比较、长期运行更清楚”的研究平台。
  - 评估页新增了“实验对比窗口”，最近训练快照、最近推理快照、最近两轮对比现在都受统一窗口控制，避免一页看 3 条、另一页看 10 条。
  - phase2 的剩余事项已经写成勾选 todo 放进实施计划，后续每做完一段都会直接打勾，避免再靠口头同步当前做到哪。
  - 评估页现在把“研究与执行差异”拆成两张表：一张说明当前差异条目和影响，一张说明先处理什么和下一步去哪，比之前只看一句摘要更适合排查真实落差。
  - 任务页新增了“活跃告警”和“人工接管时间线”，长期运行问题先显性化，再决定是否放更多自动化动作，避免误把异常状态当正常状态。
  - 本地 `9021/9022` 这轮已经按最新代码重启，真实页面确认 `/evaluation` 和 `/tasks` 都能直接看到新的对比窗口、告警和接管信息；前端样式资源也能正常加载。
  - 数据工作台现在会把原始样本、清洗后样本、特征就绪样本、总丢弃行、保留率和来源解释直接显示出来，避免再问“这次研究到底用了哪批数据”。
  - 特征工作台现在明确显示类别权重解释；研究工作台新增模型和标签说明；回测工作台新增成本与过滤拆解，这三页现在都更像解释型工作台，而不只是参数面板。
  - 任务页把“现在该不该停、要不要接管、能不能恢复”前置成了接管快捷动作和恢复确认，不再要求用户自己从计数和状态里拼结论。
  - 主导航统一关闭自动预取，避免在当前 Next 结构下触发一串 400 预取噪音；数据页下拉框也统一切成深色样式，浏览器级终端主题审计重新恢复通过。
  - phase2 todo 里的 7 个显性化补齐项这轮已经全部打勾，当前重点会转到更完整的标签方式、模型选择、细粒度因子配置和更深的实验对比。
  - 独立复核指出任务页“首要动作”优先级会和恢复结论打架，这轮已经统一改成以恢复结论为准；研究页的 `3-5d` 持有窗口说明也已补齐，避免把真实研究口径说错。
  - 刚刚同步了手动流水线到任务页：`/signals` 手动运行会写入 AutomationService 记录并在 `/tasks` 中显示“最近工作流来源：手动信号流水线”，按钮运行时不会再提示“客户端代理暂时不可用”。
  - `/tasks` 的“本轮自动化判断”现在改成客户端轮询 `/api/control/tasks/automation`，首屏先吃服务端结果，随后自动刷新；这样手动信号流水线不需要整页刷新，也能在任务页里看到最新来源、推荐和说明。
  - 前端验证这轮确认了一条硬规则：不要再把 `pnpm build` 和 `pnpm dev` 混跑在同一个 `.next` 目录里，否则 Next 会丢 `routes-manifest` 和 chunk 文件，页面会直接 500；现在已经改成“停 dev -> build -> 再启动”的串行验证。
  - 这轮完整验证结果：后端定向测试 `31` 通过、`0` 失败；Playwright 关键交互测试 `5` 通过、`0` 失败，扩大到登录/导航/信号/任务共 `8` 通过、`0` 失败；前端生产构建通过；真实点击登录、运行流水线、查看任务页已确认链路可用。
  - 这轮又补做了一次从 `/signals` 点击“运行 Qlib 信号流水线”的整条真实点击链：登录成功后，点击会立即进入后台运行状态，研究运行状态区会显示阶段和进度，任务页也会刷新出“最近工作流来源：手动信号流水线”。
  - 浏览器全量测试这轮暴露出一个真实验证规则：如果 `pnpm build` 和 Playwright 同时打 `.next`，页面会出现亮底控件和 400 资源错误，看起来像样式 bug，实际是构建产物被并发覆盖；现在已经固定成“build 完成 -> 重启 9022 -> 再跑全量浏览器”。
  - 主流程浏览器用例现在统一改成更贴近真实访问的 `domcontentloaded` 导航等待，并把整条跨页面链路的超时单独放宽，避免单币页在长链用例里被误报成页面超时。
  - 这轮完整验证结果：后端 API 测试 `299` 通过、`0` 失败；worker 测试 `99` 通过、`0` 失败；仓库级测试 `97` 通过、`0` 失败；Playwright 浏览器全量 `39` 通过、`0` 失败；前端生产构建通过；真实点击登录、运行流水线、查看研究运行状态和任务页来源已确认可用。
