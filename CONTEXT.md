# 当前进度

- 当前正在做：继续收口 `Freqtrade` 在阿里云上的真实 `live` 联调，重点确认真实买入、真实同步和真实卖出闭环。
- 上次停留位置：阿里云上的 `API / WebUI / Freqtrade / Mihomo` 四个容器已经稳定运行，真实余额、真实订单和真实持仓都能从服务器读到。
- 近期关键决定：
  - `WSL` 继续负责开发和测试，阿里云服务器负责真实 `dry-run / live` 验证和最终部署。
  - 服务器端口继续按 `/home/djy/.port-registry.yaml` 统一管理，`Quant` 主范围固定，临时联调用 `Quant-Debug-N`。
  - Binance 在服务器上通过固定代理节点访问；当前固定节点仍要用它的真实出口 IP 做白名单。
  - `Freqtrade` 当前已经确认：
    - 真实 `Spot + dry-run + REST` 已跑通
    - 首笔真实 `DOGE/USDT` 买单已经成交
    - `live` 同步会直接用 Binance 账户结果确认刚派发的订单
    - `flat` 只会收敛到当前币种或当前交易，不会全平
  - 这次新确认：
    - 阿里云服务器已经切到 `live` 并验证过真实余额同步
    - 服务器上的 `DOGE` 现货余额仍在 Binance 账户里
    - 但当前 `Freqtrade live` 交易库为空，`/api/v1/status` 和 `/api/v1/trades` 都返回空
    - 所以这笔 `DOGE` 现在属于“Binance 账户里有、但 Freqtrade 不认”的未托管现货仓位
    - 控制平面现在会明确报出这类未托管现货仓位错误，不再只给出模糊的“没有可平持仓”
- 当前阻塞：
  - 服务器上的真实卖出闭环还没完成
  - 不是 Binance 权限问题，而是这笔 `DOGE` 不在当前 `Freqtrade live` 交易库里
  - 下一步需要先清理这笔未托管现货仓位，再重新做一轮由当前服务器 `Freqtrade live` 自己创建并自己平掉的闭环验收
