# 当前进度

- 当前正在做：`Qlib` 已从“最小研究展示层”推进到“研究结果参与策略判断”的软门控阶段，当前处于 review 收口和 git 提交前整理阶段。
- 上次停留位置：刚完成 qlib 软门控实现、测试和真实页面验证。
- 近期关键决定：
  - 当前继续采用多 session 分工：
    - 我负责 `Qlib` 研究层
    - 另一个 session 负责 `Freqtrade` 执行层
  - 总路线不变，继续保持“适配器优先，总控自研”：
    - `WebUI + Backend + OpenClaw` 是控制平面
    - `Qlib` 是研究层
    - `Freqtrade` 是加密执行层
    - `Lean / vn.py` 先保留扩展位
  - `Qlib` 这条线当前已经分两步完成：
    - 第一步：最小训练、最小推理、结果展示
    - 第二步：研究结果以“软门控”方式进入策略判断
  - 当前软门控规则已经固定：
    - 原策略仍然是主判断源
    - `score >= 0.60` 只确认现有做多信号，不单独触发新信号
    - `score <= 0.40` 会压低原本的做多信号，把它降回 `watch`
    - `0.40 < score < 0.60` 维持观望，不让研究层单独拍板
  - 当前接口和页面已经新增两类字段：
    - `confidence`
    - `research_gate`
  - 策略页现在已经能直接看到：
    - 当前判断
    - 判断信心
    - 研究门控状态
    - 研究分数、模型版本和解释
  - 当前研究层协同边界已经明确：
    - 默认运行目录仍是 `/tmp/quant-qlib-runtime`
    - 多 session 并行时可以设置 `QUANT_QLIB_SESSION_ID`，把最近研究结果隔离到各自子目录
    - 没安装 `qlib` 时继续走 `qlib-fallback`，先保证最小研究闭环可用
  - 当前已完成的验证包括：
    - 策略引擎、策略工作台、研究服务、API 骨架相关测试通过
    - 前端静态测试、类型检查和构建通过
    - 真实 API 已返回 `confidence / research_gate`
    - 真实策略页已展示“判断信心 / 研究门控”
