# 当前进度

- 当前正在做：phase2 后半段继续收口；这轮把研究工作台的新模型、新标签、新持有窗口真正接进后端打分，并把评估页、任务页的解释继续补清楚。
- 上次停留位置：统一候选池 + `live` 子集已经落地，研究训练进度、实验对比、告警快捷处理、人工接管和浏览器全量验证都已收口。
- 当前服务：
  - API：`http://127.0.0.1:9021`
  - Web：`http://127.0.0.1:9022`

# 这轮完成

- 研究工作台补上两个新模型：
  - `momentum_drive_v4`
  - `stability_guard_v5`
- 研究工作台补上两个新标签预设和两个新持有窗口：
  - `pullback_reclaim`
  - `volatility_breakout`
  - `1-2d`
  - `2-5d`
- worker 打分逻辑已经真正消费这两个新模型，不再只是前端看得到。
- 评估页手动实验对比补上：
  - 研究预设
  - 标签预设
  - 标签触发口径
- 任务页补上“告警等级处理口径”，把错误、警告、提示各自会怎么处理直接讲清楚。

# 关键决定

- 研究推荐和 `dry-run` 继续共用更大的候选池，`live` 仍保留更严格的小子集，避免“研究推荐出来却接不上执行”。
- 前端验证继续按固定顺序执行：先 `build`，再重启 `9022`，再跑浏览器测试，避免旧构建和新页面混在一起。
- 页面说明优先讲“为什么推荐、为什么淘汰、为什么不一致、下一步去哪”，不再只堆指标。

# 最近验证

- 定向单元测试：`89` 通过，`0` 失败
- 前端生产构建：通过
- Playwright 浏览器测试：`39` 通过，`0` 失败
- 真实页面检查：`/research`、`/evaluation`、`/tasks` 已确认能看到这轮新增内容

# 下一步

- 继续把工作台从“能配一部分”补成“更完整可配置”
- 继续加强实验对比和研究结果/执行结果对齐
- 继续收紧长期运行、告警、调度和人工接管
