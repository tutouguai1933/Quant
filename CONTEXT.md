# 当前进度

- 当前正在做：`phase1` 验收阻塞点已收口，系统正式继续进入下一阶段。这轮修掉了 3 个关键问题：固定日期范围没数据时不再悄悄回退全量、评估页最近两轮对比会正确回填上下文、任务页和评估页现在统一按同一个复盘窗口展示。
- 上次停留位置：工作台已经能看、也能配一部分，但阻塞问题还没完全收干净；评估页和任务页对同一轮结果的解释也还不够一致。
- 近期关键决定和原因：
  - 工作台配置继续只走统一配置中心，不再给数据、特征、研究、回测、自动化各开一套保存逻辑，避免配置漂移。
  - 研究结果和当前配置是否一致，统一通过 `config_alignment` 和“最近两轮对比”显性化，避免只看到结论、看不到上下文。
  - 研究训练、推理、流水线统一改成后台运行，并显式给出阶段、进度、预计时长和结果去向，避免“点了按钮不知道系统在干什么”。
  - 这轮补充决定：如果实验账本缺少配置快照，页面必须明确显示“暂时无法比较”，不能再误写成“没有变化”。
  - 这轮前端已做真实页面验证：`/data`、`/evaluation`、`/tasks` 都能看到关键工作台结构；`/_next/static/css/...` 样式资源正常返回；登录后的 `/tasks` 能看到长期运行参数、`dry-run only` 和 `Kill Switch`。
