# 当前进度

- 当前正在做：`phase1` 已完成最后验收，`phase2/phase3` 正在继续推进。这轮把工作台从“能看”继续推进到“更完整可配置”：新增了固定时间窗、缺失值策略、长期运行参数、最近两轮实验对比，也把研究后台运行状态和预计时长接进了页面。
- 上次停留位置：工作台虽然已经能看、也能配一部分，但还缺三块：数据与特征配置不够完整；评估页缺少实验对比；研究训练按钮缺少明确反馈。
- 近期关键决定和原因：
  - 工作台配置继续只走统一配置中心，不再给数据、特征、研究、回测、自动化各开一套保存逻辑，避免配置漂移。
  - 研究结果和当前配置是否一致，统一通过 `config_alignment` 和“最近两轮对比”显性化，避免只看到结论、看不到上下文。
  - 研究训练、推理、流水线统一改成后台运行，并显式给出阶段、进度、预计时长和结果去向，避免“点了按钮不知道系统在干什么”。
  - 这轮前端已做真实页面验证：`/data`、`/features`、`/research`、`/evaluation`、`/tasks` 都能看到新增配置项；`/_next/static/css/...` 样式资源正常返回；登录后的 `/tasks` 能看到长期运行参数和人工接管入口。
